Fix withdrawal ledger linkage, join races, and multi-socket room membership

- Link withdrawal transactions to their requests (schema FK + migration),
  with legacy fallback adoption in approve/reject and unit tests
- Serialize concurrent room joins (Bingo + Keno) so players can't be
  pushed twice or double-charged entry fees
- Track per-socket membership on player entries so one tab closing can't
  eject a co-located tab, and targeted emits reach all of a user's sockets
- Register activeRoomId only after the join fully succeeds (closes the
  disconnect-during-join window that regenerated card options)
- Guard the play page auto-select to fire once and retry on stale options
- Use unique keys in the keno history table and clean historical dup ticket
- Remove StrictMode double-mount churn from the Bingo join effect
